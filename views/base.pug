doctype html
html
    head
        include _head.pug
    body
        // HEADER
        include _header.pug



        // CONTENT
        block content
        //    h1 This is a placeholder. The data is coming from block content


        // FOOTER
        include _footer

        // Load axios CDN first (global dependency)
        //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.11.0/axios.min.js')
        //- script(src="/js/leafletMap.js")
        //- Main Bundle
        script(src='/js/bundle/index.js')
        link(rel='stylesheet' href='/js/bundle/index.css')

        // Load page-specific scripts - each of the script will be injected from its block append scripts
        //- block append scripts/ page.pug = page-specific scripts (like components/hooks in frontend)
        //- block scripts
