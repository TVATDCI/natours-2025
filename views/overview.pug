extends base

block content
    section.overview
        h1= title
        //- title = all tours 
        h2 Our Exciting Tours
            ul
                each tour in tours
                    li #{tour.name} - #{tour.duration} days ($#{tour.price})
        br
        a(href="/tour") Click here for more details!